# 更新日志

版本号遵循 SemVer，与 git tag 一一对应（`v0.2.0` → `0.2.0`）。发布流程见 [docs/发布与自动更新.md](docs/发布与自动更新.md)。

## 未发布

- 设置窗口：排除应用的提示与列表之间留出间距
- 设置窗口：去掉滚动条（改为在 AppKit 层摘除 `NSScrollView` 的 scroller），滚动照常
- 焦点：本 app 自己的窗口拿着键盘时，焦点窗口的洞让位——不再出现「设置窗口 + 上一个窗口」两个都清晰
- 挖洞圆角统一：自有窗口与排除应用的窗口不再用写死的 12，改跟 `cornerRadius`
- 文档：发布与自动更新方案；CI / 发布工作流；版本注入脚本

## 0.1.0

首个可用版本。

- 常驻菜单栏，启用后只让当前焦点窗口保持清晰，其余全部高斯模糊
- 两种后端：ScreenCaptureKit 截图 + `CIGaussianBlur`（精确，需屏幕录制权限），未授权时自动降级到系统模糊
- 焦点挑选分层：系统对话框 → 最前 app 自己的窗口 → 其他可用窗口
- 多显示器：每张屏一层覆盖，洞跟着焦点窗口走
- 鼠标周围保持清晰（可选）、全屏时停止模糊、菜单栏与 Dock 保持清晰（可选）
- 排除应用：加入列表的应用窗口始终保持清晰
- 省电：桌面静止降频、无焦点窗口时完全停止、休眠/锁屏暂停
- 设置窗口：通用 / 外观 / 行为 / 关于 四个分页 + 全标签搜索
